when:
  - event: manual

steps:
  dagger-unix-socket:
    image: registry.dagger.io/engine:v0.19.11
    commands:
      - test -S /run/dagger/engine.sock
      - _EXPERIMENTAL_DAGGER_RUNNER_HOST=unix:///run/dagger/engine.sock dagger version
