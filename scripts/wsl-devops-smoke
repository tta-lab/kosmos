#!/usr/bin/env bash
set -euo pipefail

if ! command -v kosmos-devops-gate-status >/dev/null 2>&1; then
  echo "missing required command: kosmos-devops-gate-status" >&2
  exit 1
fi

exec kosmos-devops-gate-status --strict
