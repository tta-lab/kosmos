#!/usr/bin/env bash
set -euo pipefail

repo_root="${KOSMOS_REPO_ROOT:-$(pwd)}"
rendered="$(mktemp)"
trap 'rm -f "$rendered"' EXIT

tk show --dangerous-allow-redirect "$repo_root/tanka/environments/devops" \
  | yq -s '.' >"$rendered"

jq -e '
  (map(select(.kind == "ConfigMap" and .metadata.name == "canonical-gateway")) | first | .data.Caddyfile) as $caddy |
  (map(select(.kind == "ConfigMap" and .metadata.name == "coredns-custom")) | first | .data["kosmos.override"]) as $dns |
  ($caddy | contains("@memos host memos.localhost")) and
  ($caddy | contains("reverse_proxy memos.notes.svc.cluster.local:5230")) and
  ($dns | contains("rewrite name exact memos.localhost canonical-gateway.devops.svc.cluster.local"))
' "$rendered" >/dev/null

printf 'canonical gateway routes the private Memos host\n'
