#!/usr/bin/env bash
set -euo pipefail

repo_root="${KOSMOS_REPO_ROOT:-$(pwd)}"

bash "${repo_root}/scripts/devops-gate-status" --help | grep -q 'usage: devops-gate-status'
bash "${repo_root}/scripts/devops-gate-status" --help | grep -q 'FORGEJO_SMOKE_TOKEN_FILE'
bash "${repo_root}/scripts/devops-gate-status" --help | grep -q '/home/neil/.config/kosmos/forgejo-smoke-token'
bash "${repo_root}/scripts/devops-gate-status" --help | grep -q 'kosmos-dagger-engine-config-smoke'
bash "${repo_root}/scripts/devops-gate-status" --help | grep -q 'kosmos-cloudflared-ingress-smoke'
grep -q 'sudo -n test -s' "${repo_root}/scripts/devops-gate-status"
grep -q 'cloudflared-tunnel-kepos.service' "${repo_root}/scripts/devops-gate-status"
grep -q 'unit_enabled' "${repo_root}/scripts/devops-gate-status"
grep -q 'podman-dagger-engine.service' "${repo_root}/scripts/devops-gate-status"
